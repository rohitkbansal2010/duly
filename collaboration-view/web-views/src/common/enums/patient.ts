export enum Level {
  HIGH = 'High',
  MEDIUM = 'Medium',
  LOW = 'Low',
}

export enum HealthJourneyType {
  APPOINTMENTS = 'appointments',
  VISIT_SUMMARY = 'visit-summary',
  MEETING = 'meeting',
}

export enum GoalType {
  USERS = 'users',
  CROCKERY = 'crockery'
}
